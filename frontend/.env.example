VITE_API_URL=http://localhost:4000

# Only required when VITE_API_URL points at the deployed public Function URL
# (must match backend's API_KEY). Leave unset for local dev against
# local-server.ts, which doesn't check it.
VITE_API_KEY=
